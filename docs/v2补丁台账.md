# v2 补丁台账

固定 upstream/v2：`080b7671dea45a693b537c1e358e89ab14463d0d`（2.0.12），本地分支 `v2`。本批源码已通过三组独立审查（阻塞0、应改0）；每个实际制品仍须完成其独立构建、身份和集成校验。完整证据在 `.localdoc/测试报告/`、`.localdoc/审查记录/闭环-v2*.md`。

| 范围 | 本批处理/责任边界 | 回归依据 | 退役条件 |
|---|---|---|---|
| 插件/TUI 启动与退出 | 后台初始化、真实输入可用、activation ownership及晚到贡献清理；已验 | TC-001/002、startup-stability、app-lifecycle/plugin-source | 官方等价覆盖且阻塞/晚到清理回归通过 |
| Location/局部资源失败 | 资源错误分离、last-good/retry、提交准备取消；command catalog未加载不误发普通prompt；已验 | TC-003～005、solid-data/startup/admission/command-catalog | 官方保留草稿、取消防迟到提交和错误隔离 |
| Windows rg 解压 | tar.exe产物校验/失败回退、pwsh/PS5.1环境隔离；真实冷下载及路径矩阵已验 | TC-006、ripgrep-extract、受控Git ignore下15.0/15.1对照 | 官方覆盖 PSModulePath 与中文/空格/引号路径 |
| owner-only 文件权限 | SID私有创建、Effect取消等close、原子无覆盖迁移；默认DB目录私有继承；已验 | TC-007/008、restricted token初生/重建、真实CLI graph、Bun/Node | 官方真实落盘路径 ACL 满足且不修改任意用户父目录 |
| 四项交互增强 | F4临时权限模式、Ctrl+X、助手时间戳、auto语义色；已验 | TC-009～012、真实按键/copy/POST/theme | 上游等价功能通过全部交互回归 |
| Prompt一致撤销 | 应用层文本/光标/附件统一逻辑历史，256步/16MiB；不猜native checkpoint；已验 | 54 mutator、同标签/展开/EOF/virtual snap、预算/重置 | 上游正确维护元数据与光标并覆盖全部反例 |
| Windows MCP后代 | 仅本地MCP Job Object先收容再启动，detached后代终止；普通shell不变；已验 | TC-013、真实Node/Bun、argv/env/cmd/bat/rawbytes、独立Job | 官方等价收容和关闭，不依赖失效父PID枚举 |
| Windows旧边缘补丁 | 当前Bun1.4.2/Git2.55未复现旧spawn/kill挂起和null-device问题，不迁移；未泛化fs retry | TC-013/014、platform-edge；关窗/真实剪贴板人工项目仍未验 | 持续用回归证据决定新增或退役 |
| Windows 构建与工具链 | build-win.ps1/windows-build.ts；固定Bun/锁/基线、原始source.diff、源码和制品hash | windows-build/links/实际patch解析；制品还须当次smoke | 官方提供等价可追溯 Windows 包装和无覆盖部署 |
| 隔离服务冒烟 | 独立根/local注册/随机端口/PID/image/hash；统一nodeHTTP直连，诱饵proxy零请求；已验 | 真实四脚本入口、认证/停止/错误清理；最终binary重新验证 | 官方隔离与身份核验及无代理凭证外发等价 |
| 更新与同版服务风险 | 官方local channel禁自动更新，无默认注册迁移；保持官方updater | TC-017、构建来源和启动身份检查 | 上游提供可信fork/update来源保护和服务制品身份 |
| 历史数据回滚 | 官方基线合成SQLite副本/47条迁移及恢复hash已验；不是日常用户历史库验证 | TC-017、compiled-integration-win；真实用户数据操作前另行备份 | 官方兼容/回滚保证及相应数据样本验证后复评 |

构建详细使用方法、安全约束和配置迁移见 [v2-Windows构建与迁移](./v2-Windows构建与迁移.md)。本台账不替代各并行开发记录和最终测试报告。

已退役：旧CLI懒加载、Location singleton/prewarm、reference/config绕行、旧PowerShell参数和2秒close兜底、skill依赖rg的降级。采用官方新实现。LSP暂缺为用户已接受边界，不恢复旧包。

兼容边界：Windows显式自定义DB要求父目录事先私有（不会自动修改）；Windows迁移使用同卷hardlink；受限PowerShell/Job企业策略、非Windows实机、真实终端/IME/剪贴板和云端CI未全部验证。App E2E专用类型检查仍有三处既有未改文件错误；根标准检查通过，不代表全仓所有E2E通过。
